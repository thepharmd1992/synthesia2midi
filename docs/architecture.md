# Architecture

The canonical architecture document is now [`../ARCHITECTURE.MD`](../ARCHITECTURE.MD).

This file remains as a stable link for older references. Keep substantive architecture updates in the root `ARCHITECTURE.MD` so agents can find the repo map from `AGENTS.md` without digging through `docs/`.
